#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
int main() {
	int ch = getchar();
	printf("%d", ch);
	//
	//if (ch - '0' >= 0 && ch - '9' <= 0) {// integer
	//	printf("")
	//} else if (ch - 'a' >= 0 && ch - 'z' <= 0) { // 
	//	
	//} else { // capital

	//}
	system("pause");

	return 0;
}